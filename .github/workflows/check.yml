name: check
on:
  push:
    branches: [main]
  pull_request:
jobs:
  phoenix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/check.mjs .
      - run: node --test tests/*.test.mjs
